apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-insights-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/thimanshu756/healthsync.git'
    path: platform/charts/ai-insights-service
    targetRevision: feature/phase1-core-infra
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: app-ai-insights-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
