---
# ExternalName services in ingress-nginx namespace to proxy to backend namespaces
apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: patient-service.app-patient-service.svc.cluster.local
  ports:
  - port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: appointment-service.app-appointment-service.svc.cluster.local
  ports:
  - port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: frontend.frontend.svc.cluster.local
  ports:
  - port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: billing-service.app-billing-service.svc.cluster.local
  ports:
  - port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ai-insights-service
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: ai-insights-service.app-ai-insights-service.svc.cluster.local
  ports:
  - port: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthsync-ingress
  namespace: ingress-nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, PATCH, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "Content-Type, Authorization"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      # /api/billing → billing service at /billing
      - path: /api/(billing)(/.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: billing-service
            port:
              number: 80
      - path: /api/(billing)$
        pathType: ImplementationSpecific
        backend:
          service:
            name: billing-service
            port:
              number: 80
      # /api/insights → ai-insights service at /insights
      - path: /api/(insights)(/.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: ai-insights-service
            port:
              number: 80
      - path: /api/(insights)$
        pathType: ImplementationSpecific
        backend:
          service:
            name: ai-insights-service
            port:
              number: 80
      # /api/patients → patients service at /patients
      - path: /api/(patients)(/.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: patient-service
            port:
              number: 80
      - path: /api/(patients)$
        pathType: ImplementationSpecific
        backend:
          service:
            name: patient-service
            port:
              number: 80
      # /api/appointments → appointment service at /appointments
      - path: /api/(appointments)(/.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: appointment-service
            port:
              number: 80
      - path: /api/(appointments)$
        pathType: ImplementationSpecific
        backend:
          service:
            name: appointment-service
            port:
              number: 80
      # Frontend catch-all
      - path: /(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: frontend
            port:
              number: 80
